# Portfolio

Jekyll site for GitHub Pages (custom layout, no theme).

- `index.md` – page content
- `_layouts/default.html` – HTML shell (header, nav, footer)
- `assets/css/style.css` – all styling (light + dark mode)
- `assets/img/` – downscaled thumbnails (~100–200 KB instead of 4 MB PNGs)
